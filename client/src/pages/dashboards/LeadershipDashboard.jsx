import DashboardShell from "../../components/common/DashboardShell";

// Filled in by "Leadership dashboard" — company-wide figures and the four main
// units, with no names and no review content behind any figure.
export default function LeadershipDashboard() {
  return (
    <DashboardShell
      title="Company overview"
      description="Company-level figures, so you can see where teams need support."
    />
  );
}
